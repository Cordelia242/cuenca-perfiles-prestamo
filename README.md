# Perfiles de préstamo para Cuenca

Catálogo público de perfiles de préstamo consumido por la app Cuenca.

El formato está versionado en `catalogo.json`. Cada perfil incluye `nombre`, `categoria` y `tags` como strings, además de sus condiciones financieras.
